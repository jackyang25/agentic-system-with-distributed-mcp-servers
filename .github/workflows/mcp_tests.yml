---
name: MCP Tests

on:
  push:
    branches:
      - "main"

jobs:
  mcp_tests:
    runs-on: "ubuntu-latest"
    steps:
      - name: "Checkout code"
        uses: "actions/checkout@v4"

      - name: "Install Poetry"
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: "Setup Python"
        uses: "actions/setup-python@v5"
        with:
          python-version: "3.13"
          cache: "poetry"
          cache-dependency-path: "./poetry.lock"

      - name: "Install dependencies"
        run: "poetry install --no-root"

      - name: "MCP Tests"
        run: "poetry run python -c 'from mcp_integration.adapter import adapter; print(\"MCP Integration imports successfully\")'"
